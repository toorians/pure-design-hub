# TODO

- [ ] app/home/page.tsx: TestimonialSlider (infinite auto-play + drag) me jerk/vanish cause isolate
- [ ] Replace render-time window.innerWidth usage with deterministic client-side width state
- [ ] Stabilize teleport/jump logic: transitionEnabled + setTimeout => rAF-based update to avoid flicker
- [ ] Add cleanup/throttle for ScrollTrigger.refresh to prevent layout jump side-effects
- [ ] Run dev server / manual test: drag prev/next + auto-play loop, check no vanish/jerk

